# Gero 2.7 — Hide+Gate Removal Plan (Midnight / Bitcoin / Pool-Operator)

_Generated 2026-06-24 from workflow wf_fd030b57-4b3 (gero-2.7-audit)._

> Phased HIDE+GATE plan for Gero Wallet 2.7 (branch midnight-csp-indexer-host) covering three features in dependency-safe order. Phase 0 (Midnight) is a clean delete of orphans — independent of everything. Phase 1 (Bitcoin + thorchain/ordinals/gomining/babylon/mempool/lightning) is the heavy one: the chain registry in src/utils/networks.ts is the single master gate; removing the two Bitcoin entries auto-disables every resolve*Support() helper, which cascades to nav, dialogs, and dApp surfaces. The only real GAP is six ungated direct-URL routes in router.ts. Phase 2 (Pool Operator/SPO) is a feature-flag hard-pin plus nav + ContentLayout polling gate. Total effort: SMALL-to-MEDIUM, roughly 3-4 hours including typecheck/build/click-through. Phase 0 ~20 min, Phase 1 ~2 hr (most of it verification of the cascade), Phase 2 ~40 min. Critical ordering rule: gate the chain registry (networks.ts) and feature flags BEFORE touching nav/router, so the support-helpers already return false when downstream code reads them. All code is retained per HIDE+GATE — only access is severed.

---

## Gero Wallet 2.7 — HIDE+GATE Execution Plan (branch: midnight-csp-indexer-host)

Strategy: keep all code in-repo, sever USER ACCESS at the highest-leverage gate points (chain registry, feature flags, nav, router). Verified every line reference below against live code.

### Global ordering rule
1. Gate the **master switches first** (chain registry `networks.ts`, feature flags) so every downstream `resolve*Support()` / `is*Enabled()` reader already returns false.
2. Then close **direct-URL route gaps** in `router.ts` (nav-hiding alone does not block `#/thorchain`).
3. Then optional belt-and-suspenders (dApp injector, ContentLayout polling).
4. Midnight (Phase 0) is fully independent — do it any time; it is pure orphan deletion.

---

## Phase 0 — Midnight remnants (clean delete; independent) — ~20 min

Verified: no routes, nav, registry, stores, deps, manifest, or i18n reference Midnight. The `midnight-drop` carousel id is an orphan (no item ever built with it, confirmed in PortfolioPage.vue live route + Dashboard.vue dead code).

- [ ] `src/modules/dashboard/components/FeatureCarousel.vue:184` — delete `if (item.id === 'midnight-drop') classes.push('midnight-background');` (dead branch). **Leave line 185 `apex-welcome` alone — still used.**
- [ ] `src/modules/dashboard/components/FeatureCarousel.vue:407-410` — delete the `/* Midnight specific styles */` comment + `.midnight-background { ... }` rule. **Leave `.apex-welcome-background` (412-415).**
- [ ] `src/assets/Midnight.png` — delete (orphan; only the CSS comment referenced it).
- [ ] `src/shared/utils/resolver.ts:356-357` — change `TOKEN_IMAGE_OVERRIDES` to `isServiceWorker ? {} : {}` (drop the `'NIGHT'` entry). `applyTokenImageOverride()` (364-367) works against an empty map. OPTIONAL — harmless to leave; only swaps an icon if a token named "NIGHT" ever appears.
- [ ] `src/utils/assets.ts:90` + `:180` — remove `import nightTokenSvg ...` and the `nightTokenSvg,` re-export. **Only if you did the resolver edit above** (otherwise build breaks — nothing else consumes it).
- [ ] `src/assets/svg/night-token.svg` — delete **only after** the assets.ts import is removed.

Note: items 4-6 are a bundle — do all three or none. Minimal-churn option: skip 4-6 entirely (the NIGHT override is inert unless such a token appears).

---

## Phase 1 — Bitcoin + thorchain/ordinals/gomining/babylon/mempool/lightning — ~2 hr

### Step 1.1 — MASTER GATE: chain registry (do this first)
- [ ] `src/utils/networks.ts:236-333` — remove both Bitcoin entries (`Bitcoin Mainnet` 237-284, `Bitcoin Testnet` 286-333) from the `networks` array. This single edit:
  - Removes BTC from CreateWallet / RestoreWallet / PairHardwareWallet / NetworkSelector (all iterate `networks.networks`).
  - Makes `resolveThorchainSupport`/`resolveOrdinalsSupport`/`resolveGoMiningSupport`/`resolveBabylonSupport`/`resolveMempoolSupport`/`resolveLightningSupport` (lines 404-439) return false everywhere — they only ever returned true for Bitcoin entries (verified: `goMiningSupport`/`thorchainSupport`/etc. are `true` only on the BTC objects).
  - Makes `resolveNetwork('Bitcoin',…)` return undefined → stored BTC wallets drop out of UI lists.
- [ ] `src/utils/networks.ts:6` — `import bitcoinLogo` becomes unused. Leave it (code-in-repo, compiles fine) OR delete. Either works.

Safe: every consumer defaults to `networks.networks[0]` (Cardano Mainnet); nothing indexes a BTC slot.

### Step 1.2 — Feature flag force-off (defense in depth)
- [ ] `src/stores/featureFlagsStore.ts:125-127` — hard-pin `isGoMiningEnabled(): boolean { return false; }`. Double-gates gomining regardless of remote LaunchDarkly flag.

### Step 1.3 — Close the direct-URL route GAP (the only real gap)
- [ ] `src/modules/navigation/router.ts:383-390` (`routeNetworkGuards`) — add six entries so direct hits redirect to `/`:
  - `gomining: (c,n)=>networks.resolveGoMiningSupport(c,n)`
  - `babylon: (c,n)=>networks.resolveBabylonSupport(c,n)`
  - `ordinals: (c,n)=>networks.resolveOrdinalsSupport(c,n)`
  - `thorchain: (c,n)=>networks.resolveThorchainSupport(c,n)`
  - `mempool: (c,n)=>networks.resolveMempoolSupport(c,n)`
  - `lightning: (c,n)=>networks.resolveLightningSupport(c,n)`
  - With Step 1.1 done, all return false → redirect. Without this, `#/thorchain` etc. (routes at router.ts:240-292, which have NO guard today) load lazily and can throw on a Cardano wallet.

### Step 1.4 — dApp provider surface (stop advertising BTC to web pages)
- [ ] `src/chrome/inject.ts:113-132` — gate `window.gero_btc` (Tomo provider) behind `const BITCOIN_DAPP_ENABLED = false`.
- [ ] `src/chrome/inject.ts:308-329` — gate `_registerGeroWalletStandard()` IIFE behind the same flag.
- [ ] `src/chrome/inject.ts:335-348` — gate `_registerBtcProvider()` (WBIP-004 `window.btc_providers`).
  - Background `BITCOIN_METHOD.*` handlers already reject with "Not a Bitcoin wallet" when `currentWallet.chain !== Blockchain.BITCOIN`, so this is HIDE-cleanliness, not a safety fix.
- [ ] `src/chrome/webpage.ts:206-296` — OPTIONAL: gate the Unisat-compatible window-side API. Only reachable if a page already discovered the provider (now gated in inject.ts). Leave for code-in-repo unless zero BTC symbols on page is required.

### NO-EDIT (auto-inert after Step 1.1 — listed so reviewers don't touch them)
- NavigationDrawer.vue:344-387 (6 BTC nav items, all `enabled: networks.resolve*Support(...)` → false).
- QuickActionsBox.vue:163-167,215; BuyDialog/ReceiveDialog/ContentLayout/BitcoinSendDialog/etc. — all key on `loggedWallet.chain === Blockchain.BITCOIN`.
- background.ts / walletBg.ts / sync.service.ts / walletManager / priceStore / walletStore BTC handlers — all chain-guarded; dead at runtime, compile clean.
- `src/models/types.ts:83` (`CoinTypes.BITCOIN`), `:109` (`Blockchain.BITCOIN`) — **MUST stay**; every guard references the enum.
- i18n `bitcoin.*` / `gomining.*` / `navigation.thorchain` etc., assets (`bitcoin-logo.svg`, `bitcoinBg.png`) — keep (code-in-repo).

---

## Phase 2 — Pool Operator / SPO dashboard — ~40 min

Route name is `poolOperator`, link `/pool-operator`. Already partially gated via `isRouteUnderMaintenance` (router.ts:323-325 returns `!isPoolOperatorEnabled()`).

- [ ] `src/stores/featureFlagsStore.ts:132-134` — hard-pin `isPoolOperatorEnabled(): boolean { return false; }`. This is the master gate: it simultaneously disables the nav item (NavigationDrawer.vue:333 reads it) and the route (router.ts:325 via maintenance check).
- [ ] `src/modules/navigation/components/NavigationDrawer.vue:333` — already `enabled: ... && featureFlagsStore.isPoolOperatorEnabled()` → false after the flag pin. OPTIONAL: set `enabled: false` explicitly (mirrors multisig:334) for clarity. No functional change needed.
- [ ] `src/modules/navigation/router.ts:399` — the `poolOperator` maintenance redirect only fires when `featureFlagsStore.state.isInitialized` is true. On a hard page refresh before flags init, the route could briefly load. With the flag hard-pinned to false this is cosmetic, but to fully close it, add `poolOperator: () => false` to `routeNetworkGuards` (383-390) so it redirects unconditionally regardless of init state.
- [ ] `src/modules/.../ContentLayout.vue:548-565` — gate the SPO node polling behind `featureFlagsStore.isPoolOperatorEnabled()` so it stops polling when SPO is off (today it polls ungated).
- [ ] `src/modules/.../ContentLayout.vue:132-174` — gate the KES expiry bell (deep-links to `/pool-operator`) behind the same flag.

### RETAINED (HIDE+GATE — do NOT delete)
pool-operator module (20 files), `poolOperatorStore.ts`, `spo-api.ts`, `usePool*` composables, background.ts SPO handlers, `MessageTypes.ts:29-30`, i18n (217 keys), `useFeatureNotifications.ts:47`.

---

## Breakage Watch (compiles/runs today, breaks once hidden)

| # | Risk | Trigger | Mitigation |
|---|---|---|---|
| 1 | Direct-URL BTC modules reachable | `#/thorchain` etc. with nav hidden | Step 1.3 — add 6 route guards. CONFIRMED gap. |
| 2 | Stored BTC wallets vanish from UI | `resolveNetwork('Bitcoin')` returns undefined after Step 1.1 | Expected/acceptable for HIDE. Encrypted records stay on disk; `chain==='Bitcoin'` guards in walletStore prevent crashes. Confirm last-logged-wallet restore tolerates unresolved network. |
| 3 | dApp BTC discovery leak | `inject.ts` advertises Gero as BTC wallet | Step 1.4. Functionally safe without it (background rejects), but leaks capability. |
| 4 | `nightTokenSvg` import orphan | Phase 0 partial edit (resolver edited, assets.ts not) | Do Phase 0 items 4-6 as a bundle or skip all three. |
| 5 | `bitcoinLogo` unused import warning | Step 1.1 | Harmless; leave or delete (both compile). |
| 6 | poolOperator route on cold refresh | flags not yet initialized at router.ts:399 | Add `poolOperator: () => false` to routeNetworkGuards (unconditional). |

---

## VERIFICATION

### Automated
- [ ] `npm run typecheck` — must pass. Expect zero errors: `Blockchain.BITCOIN`/`CoinTypes.BITCOIN` enums retained, all guards still type-check, no code indexes a removed network slot.
- [ ] `npm run lint` — check for unused-import warnings (bitcoinLogo at networks.ts:6; nightTokenSvg if Phase 0 partial).
- [ ] `npm run build` — must succeed across all 4 vite configs (main/background/content/inject); confirm the gated inject.ts still builds.

### Manual click-through
- [ ] Create-wallet picker shows Cardano (+Apex) only — no Bitcoin Mainnet/Testnet.
- [ ] Restore-wallet and Pair-hardware-wallet network lists: no BTC.
- [ ] Options NetworkSelector: no BTC.
- [ ] Nav drawer (logged into Cardano wallet): no gomining / babylon / ordinals / thorchain / mempool / lightning / poolOperator items.
- [ ] Direct URL `#/thorchain`, `#/ordinals`, `#/gomining`, `#/babylon`, `#/mempool`, `#/lightning`, `#/pool-operator` → each redirects to `/`.
- [ ] Carousel: no Midnight background/tile; Apex welcome tile still renders correctly.
- [ ] Feature flags: gomining + poolOperator off regardless of LaunchDarkly state.
- [ ] dApp test page: `window.gero_btc` / `window.btc_providers` undefined (if Step 1.4 done); Wallet Standard does not list BTC features.
- [ ] No console errors on login with a Cardano wallet; ContentLayout no longer polls SPO nodes (network tab).
- [ ] (If a BTC wallet existed) login still works, BTC wallet simply absent from lists — no crash.

### Item index

| Sev | Title | Location | Recommendation |
|---|---|---|---|
| low | Phase 0 — Midnight orphan deletion | src/modules/dashboard/components/FeatureCarousel.vue:184,407-410; src/assets/Midnight.png; src/shared/utils/resolver.ts:356-357; src/utils/assets.ts:90,180; src/assets/svg/night-token.svg | Do first; fully independent. Treat resolver/assets/svg as an all-or-none bundle to avoid an orphan-import build break. Verified midnight-drop id is never constructed in live PortfolioPage.vue or dead Dashboard.vue. |
| high | Phase 1 Step 1.1 — Master gate: remove Bitcoin chain-registry entries | src/utils/networks.ts:236-333 (line 6 import) | Do this BEFORE any nav/router edits so downstream readers already see false. Leave bitcoinLogo import (line 6) — compiles either way. No code indexes a BTC slot; all consumers default to networks[0] = Cardano Mainnet. |
| low | Phase 1 Step 1.2 — Force gomining feature flag off | src/stores/featureFlagsStore.ts:125-127 | Cheap defense-in-depth; not strictly required once the BTC network entry is gone but makes intent explicit. |
| high | Phase 1 Step 1.3 — Close direct-URL route gap (CONFIRMED GAP) | src/modules/navigation/router.ts:383-390 | Must fix — hiding nav alone does not block these routes; the lazy chunks would load on direct hit and could throw on a Cardano wallet. After Step 1.1 all six guards return false → redirect to /. |
| medium | Phase 1 Step 1.4 — Gate BTC dApp provider injection | src/chrome/inject.ts:113-132,308-329,335-348 (optionally webpage.ts:206-296) | Recommended for a clean HIDE (stops advertising BTC capability). webpage.ts surface is optional — only reachable post-discovery, which is now gated. |
| medium | Phase 2 — Pool Operator / SPO hide+gate | src/stores/featureFlagsStore.ts:132-134; src/modules/navigation/components/NavigationDrawer.vue:333; src/modules/navigation/router.ts:399/383-390; ContentLayout.vue:548-565,132-174 | Retain all 20 pool-operator files, store, spo-api, composables, background handlers, MessageTypes:29-30, i18n. Flag pin is the high-leverage point; ContentLayout polling gate stops unnecessary network traffic. |
| medium | Verification & breakage watch | npm run typecheck / build / lint + manual click-through | Block release on typecheck+build green and the direct-URL redirect checks (the highest-risk gap). Confirm last-logged BTC-wallet restore path does not crash on unresolved network. |

